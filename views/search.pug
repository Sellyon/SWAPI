extends layout


block content
	script
		| window.props = {}
		| window.props.apiData = !{JSON.stringify(apiData)}
		| window.props.attributesContent = !{JSON.stringify(attributesContent)}
	h1= title
	p Welcome to #{title}